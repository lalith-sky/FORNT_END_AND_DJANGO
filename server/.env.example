# Example env for the model proxy server
# Set the default model the proxy will use when clients don't provide one
DEFAULT_MODEL=claude-sonnet-3.5

# The provider API endpoint to proxy requests to (replace with your model provider endpoint)
PROVIDER_API=https://api.example.com/v1/generate

# API key for the provider
PROVIDER_API_KEY=your_api_key_here

# Server listen port (optional)
PORT=5000
